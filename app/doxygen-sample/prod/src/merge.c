#include <merge.h>

/* 外部利用者が参照する Doxygen コメントは、ヘッダーに記載 */
/**
 *  @details
 *  ◆ここには、グループ配下のこのファイルの実装上の記載を行います。\n
 *  brief は定義済みのため、記載しません。
 *
 *  @par            実装メモ
 *  定義側 (ソース) に記載した実装メモです。
 *
 *  @par            使用例
 *
    @code{.c}
    int result = merge_in_group(1);
    @endcode
 */
int merge_in_group(int value)
{
    return value;
}

/* 外部利用者が参照する Doxygen コメントは、ヘッダーに記載 */
/**
 *  @details
 *  ◆ここには、このファイルの実装上の記載を行います。\n
 *  brief は定義済みのため、記載しません。
 *
 *  @par            実装メモ
 *  定義側 (ソース) に記載した実装メモです。
 *
 *  @par            使用例
 *
    @code{.c}
    int result = merge(1);
    @endcode
 */
int merge(int value)
{
    return value;
}
